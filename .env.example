# Xeris Network
XERIS_NETWORK=testnet

# Server
PORT=3000
JWT_SECRET=change-this-to-a-random-secret

# Pinata IPFS (optional — falls back to local storage)
PINATA_API_KEY=
PINATA_SECRET_KEY=

# Server keypair (JSON string, optional — auto-generates if missing)
# SERVER_KEYPAIR=

# Platform fee for marketplace trades (percentage)
PLATFORM_FEE_PERCENT=2.5
